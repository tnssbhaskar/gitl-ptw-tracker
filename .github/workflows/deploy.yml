name: Deploy to Firebase
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Firebase
        run: npx firebase-tools@latest deploy --token "${{ secrets.FIREBASE_TOKEN }}" --non-interactive